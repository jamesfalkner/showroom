apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  labels:
    app.kubernetes.io/name: workshop-mcp
    app.kubernetes.io/component: mcp-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
  selector:
    app.kubernetes.io/name: workshop-mcp
    app.kubernetes.io/component: mcp-server
